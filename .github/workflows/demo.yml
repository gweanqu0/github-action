name: Demo pipeline

on: 
    push:
        branches:
            - dev

    pull_request:
        branches: [main, dev]  
    schedule:
       -   cron: "*/1 * * * *"   
       
jobs:

        setup_job:
          runs-on: ubuntu-latest # macos-latest , w
          steps: 
            name: create file
            run: touch github-action.txt 
            name: print hello world
            name: desplay readme file
            run: cat README.md 
        
            build_job:
                runs-on: windows-latest
                steps: 
                    - name: display hello
                    run: echo hello
        deploy_job: 
            runs-on: macos-latest
            steps:
                -run: echohello mac       